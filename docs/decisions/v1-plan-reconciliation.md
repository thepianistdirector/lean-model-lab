# v1.0 canonical plan reconciliation — 8 September 2026

Lucas Santana's dispatch `tanduna-v1-research-20260908-lean-model-lab` supersedes the 0.4 delivery ceiling. The [v1 contract](../V1-CONTRACT.md) defines the supported product, finite campaign and observable acceptance. The current objective is an externally usable proposal-to-experiment platform and three substantive reproducible quality-and-cost findings, including a real candidate-mechanism investigation. The plan records this dispatch once; it does not create another goal or launch investigators.

## Inventory and preserved evidence

The canonical plan contains **249 tasks, 32 waves and 472 dependency edges**. The fourteen previously unstarted Wave 30/31 rows are re-scoped to v1.0. All remain **PLANNED / NOT TESTED**, with empty evidence and null platform IDs. This reconciliation supplies no runtime, research, package, public or human completion evidence.

All 235 earlier task objects are unchanged, including their acceptance, status, evidence, dependencies and coverage. All 442 earlier edges remain. Their canonical JSON SHA-256 is `9f3e650c962362161a7e8b07d117a2bc7de5bbc44dda69f022959891f1ba583c`. The immutable 27-task/71-edge historical snapshot and its five registered file hashes are unchanged. Source mappings and prior publication evidence remain intact; no broad historical task is marked DONE to make v1 eligible.

The full fourteen prior task definitions, their two waves and prior priority/scope are retained in `plan/tasks.json` under `supersededScopes`, separate from active acceptance. Their canonical JSON SHA-256 is `552012f8befbd48a42ab30ceaf40bee89617ad5b64d82e4d6dd27cdab50e35e0`. The [earlier research plan decision](research-mission-plan-20260908.md) remains a dated record. Prior execution packets are retained as historical instructions; `v1Delivery` and the exact new dispatch packet identify current delivery authority. Reusing an ID does not transfer completion from its earlier scope.

## Bounded v1 mapping

| Task | Current acceptance outcome |
| --- | --- |
| W30-T01 | Source-backed mechanism catalog with lawful acquisition and evidence limits. |
| W30-T02 | Explicit supported model/workload/candidate/evaluator admission. |
| W30-T03 | Supported compositions and strong reference/constituent controls. |
| W30-T04 | Causal candidate proposal and decisive falsification design. |
| W30-T05 | Frozen independent quality, finite study design and complete attempt/cost accounting. |
| W30-T06 | Immutable executable study, source and evaluation identities. |
| W30-T07 | Actual bounded candidate implementation and real experiment through the product. |
| W30-T08 | Root demonstrates the documented full workflow, including inspection/export/rerun and recovery, before investigator dispatch. |
| W31-T01 | Substantive quality-constrained efficiency benchmark or informative negative result. |
| W31-T02 | Substantive candidate-mechanism study with causal ablations and untouched confirmation. |
| W31-T03 | Distinct generalization and full-system-cost challenge. |
| W31-T04 | Separate verified Astra skeptical reproduction after all three packages exist. |
| W31-T05 | Actual prepared package verified in a clean supported environment. |
| W31-T06 | Exact release/research artifacts prepared, with owner approval and public/external/human gates tracked separately. |

Wave 30 enters through the unchanged foundation and builds its own bounded capability chain. The three investigations require the actual W30-T08 product demonstration. Study one and candidate study two can proceed independently. Study three may select its question from study one or separately retained study two development evidence; it does not need study two's confirmation and must not tune on it. Study three freezes its own independent untouched confirmation. The skeptical assignment depends on all three result packages; clean package completion depends on the demonstrated workflow and reproduction.

The whole aspirational analytical, training, advanced-kernel, optimizer or remote-orchestration programme is not an entry requirement. Required numerical/reference controls, lawful resources, independent quality, complete costs, evaluator protection and resource bounds are explicit within each experiment. Training remains a distinct mission and unchanged historical programme: a training study or claimed advance requires an actual implemented/traced training path with matched exposure and quality evidence. Inference controls or a toy mathematical fixture do not substitute.

The supported v1 contract presently specifies Linux x86_64, Python 3.12+, the pinned llama.cpp CPU build and already admitted official Qwen2.5 0.5B or 14B FP16 profiles. Actual tested environments must be evidenced. Registered reviewed candidate functions and mutation detection must not be represented as a security boundary against a malicious same-user writer. The product must reject unsupported execution and state whether separation is technically enforced or a workflow convention.

## Investigator and product feedback gates

Only root dispatches agents, after demonstrating usable product work. Each investigator must be verified as `gpt-6-astra`, remain a leaf, receive a bounded question, exact product entrypoint, strong comparator, lawful scenario/model rights, measurements, finite budget, disjoint output directory and falsifiers. Investigations operate the built product, retain its real outputs and cannot replace it with a scratch implementation. A separate reviewer may use independent checking code against those outputs.

A useful-study blocker must include reproduction steps, expected/actual behavior, missing capability, scientific/user impact and a minimal acceptance check. Root fixes the product, performs relevant regression/end-to-end checks, identifies the revised build and sends the investigator back to repeat or extend the experiment. Earlier build evidence remains. Shared-host expensive experiments are queued; concurrency slots do not authorize multiple heavy jobs.

Three settings, screenshots, unit passes, documents splitting one result or illustrative fixtures do not count as three findings. Each substantive result retains claim/context, provenance and redistribution rights, exact code/build/runtime identity, executable specification/rerun, raw all-attempt inventory, baseline/independent checks, evidence-derived figures, uncertainty/limitations, reproduction outcome and a cited report. A rigorous negative or inconclusive result may qualify; novelty, large gains or training progress are not predetermined outcomes.

## Resources, frozen studies and release states

The running frozen main 14B control continues unchanged. Its earlier quality/parity failures and frozen gates remain evidence. The previously owner-deferred unstarted paced/burst definitions remain unchanged; no shortened schedule is relabeled as their execution. Distinct prospective scenarios require explicit new study identities and their own frozen rules.

The existing allocation remains: at most 12 compute threads, 60,000,000,000 aggregate RAM bytes, 200,000,000,000 project disk bytes, one heavy job and monotonic deadline `310468865220540` (approximately 9 September 01:28 UTC). No clock reset, allocation extension, new paid API/GPU/cloud spend or exclusive ownership of the shared host is inferred. Per-study budgets and stopping rules are frozen within the remaining envelope; unfinished or resource-limited acceptance stays unmet.

`v1Delivery.releaseStates` separates local preparation, clean package verification, three research packages, skeptical reproduction, exact owner version/destination approval, native submission/acceptance, public release, external-person verification and qualified human review. All v1 evidence gates are pending or untested at reconciliation. The earlier native/public snapshot remains in `publication` as dated evidence and is not overwritten with invented v1 state.

W31-T06 requires concrete source/standalone application/evidence artifacts, release notes, authentic product screenshots/demo, findings overview, first-run guide, contributor opportunities and English launch copy with the repository and correct Tanduna links. Root requests approval only for exact ready versions, hashes and destinations. Local preparation is not public release or overall programme completion. No new public commit/post/release, Tanduna update, participant contact or paper submission is authorized here. After applicable explicit approval, root verifies the actual authorized public artifact/access and Tanduna readback; any applicable external/human gates remain separate. Independent authorized work continues while approval is pending.

## Planning validation

This leaf verified its actual model as `gpt-6-astra` with high reasoning. Its work is limited to the plan, validator, generated projections and this decision record; no model execution, browser, heavy suite, publication or delegation is part of this packet.

The validator adds the explicit 1.0 horizon and exact owner dispatch reference. Three small negative controls cover an altered dispatch, duplicate dispatch packet and a changed superseded task definition. Earlier status, lineage, publication identity and prerequisite checks are retained.

- `PYTHONDONTWRITEBYTECODE=1 python3 tools/generate_plan.py` — PASS; regenerated TASKS, ROADMAP and native-plan export.
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/generate_plan.py --check` — PASS; projections current.
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/validate_plan.py` — PASS; 249 tasks, 32 waves, 472 edges and immutable 27-task/71-edge lineage.
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/validate_plan.py --self-test` — PASS; all 33 negative controls and accepted DONE baseline preservation.
- Scoped `git diff --check` — PASS.
- Independent canonical readback — PASS; all 235 earlier objects/442 edges unchanged, fourteen superseded definitions intact and all fourteen current v1 rows still PLANNED / NOT TESTED without evidence or native IDs.
