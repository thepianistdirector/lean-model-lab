# Launch audit — 7 September 2026

## Observed facts

- Native Goal active in the active owner task; read back after activation and interruption. Its objective includes actual public 0.1, external reproduction and verified native Tanduna publication.
- Sole checkout real path and Git top level: the selected Lean Model Lab checkout (exact local path retained in the private launch record). Origin: `https://github.com/thepianistdirector/lean-model-lab.git`. Startup branch `main`, HEAD `821c6364ad3a0b4d553de5be0762eee60256c6bf`, clean tracked and untracked inventory; one worktree. No competing Lean Model Lab owner found in the app task inventory. Sibling project owners are independent.
- Actual root execution record reports `gpt-6-astra`, high reasoning. Both bounded leaf agents separately verified the same actual model. Model settings were not modified.
- Host: Linux, Python 3.12.14, Node 24.20.0, 32 logical CPUs, approximately 172 GiB available memory and 1.6 TiB free disk at launch; load average 3.43/5.04/14.16. This is available capacity, not a reservation. The historical arm64/16 GiB/Python 3.14 observation describes a different host.
- Existing plan validation reproduced: 27 tasks, 9 waves, 71 edges, three DONE foundation entries. No backend or weights present in the project; no inference, human validation or release inherited.
- Local Git author configuration is absent. The historical commit author is evidence, not permission to invent or set a new identity.
- Public pages read without credentials at approximately 23:11 UTC. Project identity exposed by its actual workspace page: `prj_9fed8235f17a02a46fb64411f806a2bc`. Native task list has **0 tasks**, no pagination links; public roadmap says **no task plan published**; campaign has **0 releases**. One proposal `prp_b4937892fb5adf25392c481d939656bd`, revision 1, remains **Discussion**, not an accepted plan. Page text and digest observations are retained privately under `.cache/public-audit/`.

## Source requirements and reconciliation

The complete owner launch instruction, including its final execution-loop addition, authorizes this in-scope local build. Earlier review-only wording is stale: accepted foundation work remains accepted. The 27 historical records and original dependency graph remain in immutable lineage. Narrow inference-only successors do not complete the old training/analytical/Pareto programme.

The 0.1 contract retains 128 synthetic requests, four prompt-length strata, maximum 32 requested output tokens and concurrency 1 and 4. Actual inference and a predeclared task quality rule are mandatory. Model-free checks are preparation evidence only. Separate implementation, automated checks, measured runtime, participant review, public release and native plan publication states.

## Proposals and unresolved decisions

One exact CPU model/backend graph is being reviewed before any acquisition or installation. Default heavy execution proposal: one engine, at most two compute threads, 4 GiB resident-memory ceiling, 5 GiB project artifacts and two hours total compute, with zero paid compute. Approval is still required for the final concrete dependency/model graph and allocation. Full-wall accounting must also retain acquisition/build/preflight/recovery costs and failures.

Public GitHub release and Tanduna changes require reviewable final artifacts and destination-specific authorization. Public documentation exposes supported draft/save/submit/list plan tools; no authenticated Tanduna connector is currently available to this task. A submitted draft alone cannot satisfy public publication. Qualified reviewer and independent external environment are not allocated.

## Ownership and next bounded work

Root owns contracts, runtime, evidence, README, STATUS, architecture and release integration. One Astra leaf owns `plan/`, task/roadmap projections and plan tooling. One Astra leaf owns only `docs/decisions/backend-audit.md` for the backend review. No shared-parent or sibling writes, shared services, global installs, paid resources or public network listener are used.
