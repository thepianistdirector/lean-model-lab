# Separate agent reproduction protocol

Status: the prospective protocol below is retained; separate review and all three declared native cells are complete. The [final review](../research/reproduction-review/report.md) records 1,536 new observations, exact comparisons, the retained zero-inference admission failure, explicit continuation and complete costs. The [original dispatch receipt](../evidence/v1-separate-review-dispatch.json) preserves the exact candidate03 inputs and model verification. This is process separation on the same physical host with a shared Unix identity, not independent human or institutional validation.

The reviewer first verifies ZIP inventories, source/runtime and producer identities, public relative paths, model/data/dependency notices, executable instructions and all-attempt completeness. It runs each portable analysis from an unrelated working directory into new outputs, independently checks selected raw outputs and prompt transformations, and compares manuscript numbers with retained tables. For the candidate, it checks the original training labels, prompt-only features, fitted tree, complete calibration grid and deployed controller without replacing the trained artifact. Failed studies and both-wrong outcomes remain in every relevant denominator.

The proposed native reproduction is one complete retained cell from each investigation: the 128-question 14B concurrency-four quality confirmation; the 128-table dependency-slice candidate confirmation; and the 128-query small-model, 24-record systems case. Each has two balanced pairs, totaling at most 1,536 offered native requests. The reviewer uses the exact original producing CLI, unchanged scientific recipe, admitted weights and backend, and fresh study directories. It preserves the current allocation rather than starting another clock. Native output and timing discrepancies are reported, not repaired by outcome-driven reruns. The candidate and systems choices are fixed before their remaining results are known; this reproduction is not a new untouched confirmation campaign.

The enclosing native execution ceiling is 105 minutes, inside the unchanged common deadline `310468865220540`, memory/disk ceilings and one-heavy-job queue. Quality preserves its original twelve-thread engine; the other two cells use two threads. Before starting, the reviewer must project the remaining matrix from observed execution, include a 1.5× safety factor, and confirm it fits both the remaining execution ceiling and original clock. It reports any admission failure or incomplete cell and retains all costs. There is no automatic replacement run. The final dispatch may reduce a cell only for an explicitly recorded resource limitation; it cannot silently claim the complete matrix was reproduced.

Before reviewer dispatch or reproduction outcomes, root revised the initial 90-minute planning allowance to 105 minutes. The existing quality cell takes about 2,775 seconds, the candidate fit projects about 833 seconds for its 512-request cell, and systems H1 takes about 329 seconds. Their sum with the required 1.5× margin is about 5,906 seconds, exceeding 90 minutes and fitting 105 minutes. This changes no scientific cell, request count, quality rule or common allocation; admission will use completed confirmation timings when available. The original 12-hour deadline has not moved.

Acceptance requires an evidence-linked verdict for each package, discrepancy inventory, executable reanalysis and rerun receipts, exact artifact identities, complete native observations or an honest incomplete status, and a scoped assessment of whether each finding is substantive and distinct. Root addresses concrete product or packaging blockers, preserves earlier artifacts and requests only justified verification of the changed behavior. External-person use, qualified researcher feedback, accessibility review and public-release approval remain separate gates.

The [completed original-cell projection](../evidence/v1-reproduction-projection.json) now uses actual quality, dependency-confirmation and systems H1 archive-process durations: quality 2774.945 s, candidate 800.031 s, systems 329.082 s. Their sum is 3904.059 s; the 1.5× admission projection is 5856.088 s, below the unchanged 6,300-second execution ceiling. This pre-dispatch check is not a reservation; the reviewer must check the remaining common clock again before starting.

For the same-host review, extracted packages remain within the main workspace so they share the original model dependencies and leases. Native execution starts from that outer workspace root, as required by the product. The repository queue helper may be used only after its bytes are checked against the extracted source copy; the actual native executable is the packaged original producer. Portable reanalysis runs the extracted analysis scripts from an unrelated working directory. This records the local orchestration placement explicitly and does not represent an external clean-machine execution.
