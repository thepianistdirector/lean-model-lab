# Separate-review findings and root dispositions

The reviewer received the immutable candidate03 research ZIPs after verifying its actual Astra session and their exact hashes. Its [dispatch receipt](../evidence/v1-separate-review-dispatch.json) identifies that scope. Review corrections receive new package identities; the original candidate remains retained. The separate [review report](../research/reproduction-review/report.md) now records all three complete native repetitions and its scoped per-result verdicts. Root accepts the three local findings with the corrections and limitations below; public and human validation remain open.

| Observation | Effect on evidence | Root disposition |
| --- | --- | --- |
| The original quality confirmation queue receipt, registration and product log were named by inventory hashes but omitted from the ZIP. | A reader could reconstruct native attempt/service costs but could not independently recompute the stated queue/process envelope from its original timestamps. | Added the [exact provenance supplement](../research/quality-study/confirmation-14b/queue-provenance/README.md). All three hashes match the original inventory; wait, execution and total wall values recompute exactly. The reviewer [verified the supplement](../research/reproduction-review/quality-queue-supplement-check.json). Actual candidate04 ZIP readback now verifies all four supplement files at both the package root and source paths; final continuity verifies them again. No native run or scientific value changed. |
| Two reviewer-added isolated `-I` invocations failed to import sibling candidate analysis helpers. | Those invocations differed from the documented commands. Their failures are retained; they do not establish a failure of the documented reanalysis flow. | The documented invocations passed from the unrelated working directory. No product change or model rerun was justified. |
| The retained legacy protocol failure has 30 normalized/raw token discrepancies involving prompt-progress sentinel zeros. | This is the previously disclosed interrupted protocol failure. It remains failed evidence and cannot support parity or efficiency acceptance. | Preserve the original raw and normalized records and their adverse classification. Do not repair history or count these records as successful comparisons. |
| Root staged the final-clean publication derivatives in the main workspace while their originals remained in the separate worktree. | The candidate repetition correctly refused admission before creating a study or executing any model request. The failed process consumed 6.720491959 seconds. | [Restored both exact original producer trees](../evidence/v1-clean-producer-history-restoration.json), retaining every existing copy and the failed queue receipt. [Original archive04 predecessor admission then passed](../evidence/v1-clean-producer-admission-recheck.json). Root authorized the remaining frozen candidate/system cells with a new candidate output name and the failed process charged inside the unchanged cap; quality was not repeated again. |
| The completed quality repetition has retained prior same-recipe measurement. | Its official campaign eligibility is false. Passing descriptive per-cell checks cannot make the repetition a fresh untouched confirmation campaign. | Preserve official ineligibility and null official ratios. Report original findings and descriptive repeated-cell quality/parity/practical-effect/tail checks separately. |

The matched-record control loses the symbolic answer on all 32 fit tables, and its manuscript limits the resulting comparison accordingly. Development and confirmation use different lexical prefixes and split-derived random streams; fresh groups do not establish IID sampling or lexical invariance. The restricted grammar can be solved by its symbolic interpreter and does not establish a workload requiring general language inference. These limitations remain in the findings rather than being treated as solved by successful packaging.

The review distinguishes original observations, reanalysis of retained bytes and 1,536 new model observations. It does not supply external human evidence. All original scientific source, tables and figures remain byte-bound to the reviewed candidate03 source; the sole original-manuscript change is the disclosed appended quality queue supplement, with the original retained separately. Final delivery continuity verifies these boundaries before approval is requested.

The repeated small-model cells show substantial wall-time increases despite retained scientific-output agreement where reported. The original producer synchronously traverses the workspace during resource monitoring. That is a plausible contributor to coordinator gaps, not a demonstrated causal explanation. No measured time was replaced, no runtime was optimized during reproduction, and no repetition was relabeled as fresh confirmation. Read the actual timing tables and [cost scopes](COST-ACCOUNTING.md).
