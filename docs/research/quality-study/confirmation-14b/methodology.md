# Prospective 14B confirmation methodology

This distinct follow-up preserves every file of the completed parent benchmark package and the stopped 0.5B branch. It tests one new, root-selected seed on the already successful standard v2 key-copy workload. It is neither replacement evidence for the old 14B result nor another attempt to select a favorable 0.5B scenario.

The [protocol](protocol.json) and [freeze receipt](freeze.json) were written before the packaged recipe generator was invoked. Protocol SHA-256: `82d1d704115878f038a28069a4cb6465a2b5901dae06edf155bacc00452b23c0`. The resulting recipe identity is `73037e93588c07f1968085b0933330bacaaf13ec093d0d39cc63318344823800`; its serialized file SHA-256 is `1bb41c2538cde8652069d10409d17bfc71e5760d2ff18288acb75c0de97bcf79`. The root-selected seed is `2026090901`, with 128 requests per arm, concurrency four, two AB/BA pairs, simultaneous arrivals, a 32-token output ceiling and a 128-request queue.

The executable is archive04 `1.0.0.dev0`, SHA-256 `5f1aabf84635e33dd166ee656d5bcb5830f25a5f4f5d68433e2fd3e4498fb986`, implementation `18c28e8075ebc9e5651bf4e0a66c2fca332480b4834a7342fc49d43775a9ab32`. The actual 14B FP16 model, pinned backend binary, patch, template and model acquisition setup are unchanged. Engine settings, original allocation and original quality/tail/SLO gates were compared programmatically to the historical 14B recipe and matched. Twelve compute threads are explicitly authorized by root as an exception to the six-thread preference, inside the existing twelve-thread/one-heavy-job ceiling. The [host observation](host-before-queue.json) records prequeue load and allowed affinity; actual server affinity is independently retained in native process evidence.

Baseline disables same-slot prefix reuse; candidate enables it. Within each pair, offered requests, prompts, model, tokenizer, sampling and CPU settings match. The primary quality contract remains at least 95% exact answers per arm, no observed pairwise accuracy loss and exact input/output token parity. The prospective practical gates require at least 5% service-throughput gain and at least 5% full-attempt efficiency gain, with at most 10% p95/p99 regression for both TTFT and end-to-end latency. The native v2 evaluator exposes its original service criterion; the additional full-attempt gate is separately evaluated against this prospective protocol. The 15-second TTFT and 30-second end-to-end SLOs are not changed. Joint exact-answer/SLO goodput is reported separately, so relative efficiency cannot imply adequate interactive latency.

The fixed v2 generator produces conventional held-out examples using a distinct seed, after protocol freeze. Candidate processing necessarily sees its prompts during inference, but no observed confirmation outputs select new parameters, cases or seeds. Same-Unix access and shared modules are workflow conventions, not adversarial or cryptographic isolation. A post-execution split audit counts full-prompt overlaps against the historical measurement population; no overlap-driven resampling is allowed. Filler prefixes and task grammar intentionally remain shared. This tests a new sampled workload, not a new task family or independent physical machine.

The execution is exactly one `tools/run_queued_recipe.py` invocation, with a conservative 5,400-second wrapper timeout including queue waiting. The maximum additional native work is 512 requests, taking this investigator to at most 544 including its earlier 32-request development cell. The original common allocation starts at monotonic `267268865220540` and ends at `310468865220540`; it is not reset. Resource/identity/runtime failure, cancellation or a timeout stops the study without a successful replacement. All interrupted, failed, missing and ineligible work remains evidence. The 0.5B confirmation remains unstarted regardless of this new result.

After terminal native execution, archive04 operates `inspect`, `report` and one study-specific `workbench`; the source raw auditor checks retained SSE/normalization/tokenizer/cache consistency. `derive.py` independently recomputes all observed exact-answer counts and exact token differences and asserts agreement with packaged reports. It writes a new combined table revision including the unchanged historical studies, stopped development and this confirmation; it does not rewrite the original package. Request timing, paired effects, harmful/beneficial/wrong→wrong transitions, memory and all supplied cost intervals use the parent package's documented definitions.

There are only two new paired repetitions. Both pair effects and minimum/median/maximum values are descriptive, without IID-request assumptions, significance claims or population-risk guarantees. Full-attempt denominators include repeated verification and process load; shared setup and all-clock ledgers are separate overlapping accounting views, not additive independent totals. Historical and current allocations remain distinct. Energy, thermal and monetary measurements are unavailable; no hypothetical sensor or price is substituted.

All results and conclusions must be derived after execution. A separate skeptical reproduction remains distinct from this investigator's prospective confirmation.
