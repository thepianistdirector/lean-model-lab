# 0.1 evidence and release gates

Historical 0.1 cutoff assessment: 8 September 2026. Later owner-authorized local 0.4 progress is recorded in [STATUS.md](../../STATUS.md) and [the scope decision](../decisions/0.4-scope.md); this table preserves the earlier evidence boundary. **Local real-model candidate; public Goal incomplete.** The [measured finding](../results/cpu-20260908.md) is INELIGIBLE for an efficiency claim because quality and concurrency-4 token parity failed. Negative findings are retained; acceptance thresholds were not relaxed.

| Required outcome | Current evidence | Remaining proof |
| --- | --- | --- |
| Fixed artifacts and contract | Actual owner approval; verified model/source hashes; GGUF/tokenizer inventory; frozen trace/settings and declared GCC 8 patch | Fresh environments must verify their own acquired and built bytes |
| CPU baseline and candidate | 12 completed real arm runs, 1,536 retained requests; concurrency 1/4 and 3 AB/BA/AB pairs each | External execution of the public artifact |
| Raw request and quality accounting | 1,536 raw/normalized/tokenizer reconciliations PASS; every arm 99/128 below 95%; C4 parity FAIL | Independent methodology review; no eligible effect estimate exists |
| Full costs and recovery | Acquisition, two failed builds, protocol failure and comparison retained; actual crash/journal/restart control PASS | Original allocation is now closed with retained final export charges; external recovery workflow remains outstanding |
| Package | Frozen measured development zipapp with 12 isolated software checks; source and redacted-evidence candidate | Exact public version, authorization, publication and unauthenticated retrieval |
| Report usability | Standalone escaped HTML and semantic checks; real result report retained | Browser, keyboard, zoom and narrow-layout observations; browser could not start at this cutoff; later 0.4 browser observations are separately scoped |
| Qualified/human evidence | No participant or qualified-review observation supplied | Real consented methodology and researcher workflow observations |
| External reproduction | Local isolated processes only | Fresh authorized external environment obtains the public package and runs both arms/report/recovery |
| Rights and data scope | AGPL source/fixtures; full dependency review; model license and explicit MIT compatibility patch notice; model paths redacted with original hashes | Final assembled artifact inventory/privacy review; no weights or native backend binary redistribution |
| Long-term plan | 219 tasks, 28 waves, 411 edges; 27 source records and 71 historical edges preserved | Fresh public reconciliation before supported writes |
| Native Tanduna plan | Local canonical export only; fresh 2026-09-08 public read showed no native plan/tasks | Supported authenticated session, exact revision agreement, real review/poll decision and public readback |
| Public GitHub 0.1 | No push/tag/release | Concrete publication approval and author identity, then public access and external validation |

## Allocation and provenance

The owner decision is `../decisions/admission-approved-20260908.json`. The original `approved: false` proposal is historical, not a still-pending decision. Its single two-hour/5-GiB/4-GiB/two-thread allocation was used for acquisition, recovery, real comparison and reporting; it is never reset by a retry. A new experiment needs its own finite admission.

The source/runtime version used to measure is frozen and included with its manifest. Later audit, export and documentation work is separately inventoried. Redacted study copies preserve normalized scientific records and identical JSON findings; changed raw metadata and dependent hashes are disclosed in each `EXPORT-PROVENANCE.json`. Private originals remain intact.

`packaged-verification.json` describes the exact measured zipapp's 12 model-free checks. `../evidence/automated-tests.json` describes the integrated 99-test software suite. `../evidence/runtime-recovery.json` is actual packaged-model recovery evidence. These scopes must not be collapsed into external or human validation.

## Publication and next scope

The [publication workflow](../decisions/publication-workflow.md) records the supported GitHub/Tanduna paths and exact missing authority/capabilities. Prepare and review final source, CLI, evidence and checksums before asking for a concrete publication decision. Account authentication alone is not publication permission. Native submission alone is not plan publication.

The later owner-authorized 0.3 scope now targets a bounded portion of wave 6 for matched arrival/service semantics for offline and online workloads. Later training and analytical work retain their original prerequisites. A local cache comparison does not complete those source requirements.
