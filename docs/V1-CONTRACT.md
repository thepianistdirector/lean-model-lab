# v1.0 delivery contract and finite research campaign

Owner: Lucas Santana. Accepted 8 September 2026. Dispatch: `tanduna-v1-research-20260908-lean-model-lab`. Delivery owner: active GPT-6 Astra root. This dispatch is accepted once; subsequent delivery messages reconcile this campaign rather than duplicate it.

## Observable product promise

An external researcher can obtain the prepared source and standalone Python archive, inspect model/data rights, select an admitted CPU model and supported generated workload, bind a research proposal to a reviewed candidate implementation, freeze every study input and quality rule, execute a bounded paired study, inspect all attempts and quality/cost tradeoffs in portable HTML and JSON, recover a supported interruption, export evidence, and repeat the specification. The public workflow must work without developer credentials. Unsupported mechanisms are refused explicitly. The catalog is a source-backed starting point, never automatic evidence of local support or novelty.

The supported execution environment is Linux x86_64 with Python 3.12 (tested 3.12.14), the pinned llama.cpp CPU source/build recipe, and the already admitted official Qwen2.5 0.5B or 14B FP16 profile. Actual tested versions and resources will be reported, not inferred from compatibility claims. HTML inspection is offline and requires no model. Models and backend binaries are obtained separately under their own licenses. Source, lawful synthetic data, specifications, candidate code and evidence are included in distribution; weights are not redistributed.

## Minimum acceptance

| Gate | Observable acceptance | Current state |
| --- | --- | --- |
| Proposal to real experiment | Packaged CLI freezes a proposal, registered candidate, model/workload, independent evaluator and finite resources; actual native outputs bind those identities; unsupported paths cannot masquerade as executed research. | ACTUAL WORKFLOW VERIFIED: archive03 completed native development, report, workbench, deliberate crash/recovery and verified export; adverse 1/8 quality remains ineligible. |
| Useful mechanism support | A bounded substantive candidate executes through the same product; full baseline and causal ablations retain raw transformed inputs, outputs, decisions and preparation overhead. No arbitrary imported code is executed. | SCOPED NATIVE VERIFICATION COMPLETE: all eight candidate study cells retain 1,984 original observations, actual fitting/calibration and causal controls. The controller selects full context throughout confirmation and preserves the baseline failure. The separate review repeats the complete 512-observation dependency-slice cell; its outcome does not establish a novel architecture or useful quality-qualified advance. |
| Quality and complete costs | Frozen quality thresholds, all offered requests, output differences, latency distributions, throughput, memory, setup/search/retries and all unsuccessful attempts are inspectable. Changed-input task equivalence and same-input token parity are distinct explicit study contracts. | SCOPED NATIVE VERIFICATION COMPLETE: original frozen quality/parity rules, offered denominators, startup/failure/recovery costs and adverse results remain. Separate repeated-cell costs and timing discrepancies are retained. The final external resource record charges the continuing original allocation through its stated cutoff. |
| Three findings | Three distinct substantive packages below, each with specification, provenance, code/build/runtime hashes, raw inventory, uncertainty, limitations, figures/tables from evidence and cited technical report. No illustrative fixture counts as a finding. | THREE LOCALLY ACCEPTED BOUNDED PACKAGES: original matrices, portable analyses, figures, manuscripts and separate review are retained; each research ZIP also includes its corresponding repeated-cell export. Publication and external/human validation remain separate. |
| Reproduction | A separate verified Astra leaf repeats a meaningful bounded portion through the exact package and independently checks retained raw evidence; discrepancies stay visible. Process separation is not independent human/institutional validation. | COMPLETE IN THE DECLARED LOCAL SCOPE: a separately verified Astra leaf repeats three complete cells, 1,536 observations, through exact original producers and independently checks retained outputs. See [the report](research/reproduction-review/report.md). Same host and shared Unix identity; no human or institutional independence. |
| External first run and recovery | Clean supported local environment obtains the prepared archive without private credentials, executes full CLI→native→persist→inspect/export path and supported failure/recovery. Distinguish local isolated process/container from physical host/external person. | ACTUAL CANDIDATE03 PASS: exact archive06 and source ZIP in a new same-host worktree and fresh Python 3.12.14 venv, with 32 comparison responses plus 10 retained recovery observations; 19 commands pass. No separate physical machine or external person. |
| Interface | Authentic actual-artifact screenshots; observed keyboard operation, visible focus, 200% zoom and narrow reflow; inspect failures and costs without relying on color. | ACTUAL ARTIFACT CHECKS PASS: workbench keyboard/filter/reset/disclosure segments, focus, native 100%/200% zoom and narrow reflow; the final index represents all 23 studies and opens the full quality workbench through keyboard navigation. The 2,048-row workbench has one 496 ms load observation. Human screen-reader/accessibility review remains unperformed. |
| Distribution | Versioned source/zipapp/evidence manifests, licenses/notices, first-run/troubleshooting/contribution guide, compatibility policy, release notes, English launch copy and exact destinations prepared and verified. | LOCALLY VERIFIED: source/CLI, 23 study exports and three complete research ZIPs pass actual hashes, inventories and links. Authentic index screenshots and browser evidence are retained. The external final delivery record binds the final source commit, tested bytes and complete review ZIP; public access is not inferred. |
| Publication and external gates | Lucas approves the exact v1 artifacts/destinations, authorized release is verified publicly, Tanduna state is read back, and applicable external researcher/qualified human observations are obtained and accurately scoped. | NOT AUTHORIZED / NOT OBSERVED |

## Finite investigations

1. **Quality-constrained efficiency benchmark.** Use retained frozen 0.5B adverse evidence and the completed frozen 14B cache control, followed by the prospectively frozen 14B concurrency-four held-out study now completed; the completed separate-review report records the repeated-cell outcome and its limits. Explain when caching reduces work without passing the declared quality/parity gate. Report startup/failure cost, output differences, pair variation, tails and memory. The old 95% gate and failed parity remain unchanged. The earlier main control was not protected confirmation; describe its development history honestly.
2. **Substantive candidate-mechanism investigation.** Select one feasible evidence-grounded omission/correction mechanism after prior-art and implementation-cost review. Test a causal mechanism, with full baseline and ablations at equal offered work and declared budgets. Freeze development/confirmation separation before model confirmation. A rigorous refutation or informative limitation is acceptable. A settings sweep or model-free proof alone does not satisfy this gate. Large architectural training proposals remain research hypotheses if their actual training cannot be implemented/traced within resources.
3. **Generalization and system-cost challenge.** Challenge a promising mechanism or informative failure on different task semantics, context/arrival patterns, and the other already admitted scale when feasible. Explicitly measure where setup, lost reuse, errors, tails or recovery erase an apparent win. Keep its own held-out scenario selection separate from investigation 2. Report measured resources; energy remains unavailable absent a sensor, and estimates are labeled.

Every investigator receives a bounded question, exact built entrypoint, comparator, lawful scenario provenance, finite experiment matrix, output directory, resource ceiling and falsifiers. Investigators are verified GPT-6 Astra leaves. They must report platform blockers with reproduction commands, expected/actual behavior, missing capability, scientific/user impact and a minimal acceptance check. Root implements fixes and identifies a new build before repetitions; earlier evidence remains.

The candidate's two small feasibility branches closed adversely without a positive advance. A separate frozen negative investigation uses the original record language, 32 fit and 32 calibration tables, a matched-record development control and three actual 128-case confirmation comparisons (dependency, simple gate and fitted gate). It tests semantic preservation failures and the observed full/slice oracle ceiling; it does not search for a passing wording or model. Its total cap is 2,000 offered native requests and 180 minutes within the original allocation. The systems investigation retains two eight-query development cells and freezes three 128-query held-out cells across the admitted model scales and context sizes. Its total is at most 1,600 offered requests and 90 minutes of product execution, with conservative timing admission before each cell. Frozen package protocols contain exact seeds, hypotheses and counts; neither campaign lowers the quality threshold.

Before confirmation, each package freezes primary sources, causal question, comparator/ablations, sample selection/seeds, quality rule, practical effect criteria, uncertainty method and stopping rule. No tuning against confirmation. Candidate functions receive prompt content only, never expected answers. Within a shared Unix identity, separate modules/directories and frozen hashes provide workflow separation and mutation detection, not a security boundary against a malicious writer. The release must state that limitation; stronger isolation is a separate explicit capability.

## Existing resource envelope and stop rule

Continue the original open allocation: at most 12 compute threads, 60,000,000,000 bytes aggregate RAM, 200,000,000,000 bytes project disk and one heavy job. Original monotonic start `267268865220540`, deadline `310468865220540` (12 hours, approximately 9 September 01:28 UTC). No new clock, GPU, paid API, cloud or rental is inferred. Queue all expensive studies behind the currently running 14B control; small code/contract checks do not consume extra inference capacity. Prefer fewer threads for the small model and inspect shared-host load before each heavy job.

Per-investigation budgets are frozen after observed feasibility and before confirmation, within the remaining common allocation. No blind reruns. Stop a cell on cancellation, exhausted time/memory/disk, invalid identities/protocol or predeclared futility. Retain unfinished, invalid, failed, cancelled and unfavorable attempts. If the existing envelope cannot deliver a substantive gate, retain its actual incomplete status and surface the precise resource/input need; do not replace evidence with a promised result.

## Reconciliation and claim boundaries

This owner mandate supersedes the 0.4 delivery ceiling and authorizes routine reversible project-local implementation and dependencies within existing resources. Existing canonical evidence and historical scientific requirements remain; the whole aspirational programme is not a prerequisite to a bounded v1 release. The previously deferred unstarted paced/burst recipes remain immutable; a new investigation may justify distinct shorter prospective scenarios rather than silently relabeling those as executed.

Training studies require actual traced training. No inference benchmark establishes training progress. Synthetic structured workloads establish only their declared task family, not general language competence. Statistical descriptions and local byte verification do not establish universal speedups, novelty, peer review, independent institutional validation or external user success.

Native Goal readback on acceptance returned no active goal, despite historical STATUS text. Root created the explicitly requested v1 goal successfully. This file and STATUS maintain dispatch continuity. Local preparation, submitted review, accepted Tanduna plan, public release and human verification remain separate states. The [external review brief](release/EXTERNAL-REVIEW-BRIEF.md) defines the concrete first-use, methodology and accessibility observations still needed; nobody has been contacted. No public commits, release, Tanduna writes, outreach or paper submission is authorized by this dispatch; approval is requested only for the finished concrete package.
