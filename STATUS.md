# Current state

Last updated: 2026-09-09. Maintainer: Lucas Santana ([thepianistdirector](https://github.com/thepianistdirector)). The canonical task/evidence ledger is plan/tasks.json; this file summarizes observed evidence and gates. TASKS.md and ROADMAP.md are generated views.

## Active v1.0 owner mandate

Dispatch `tanduna-v1-research-20260908-lean-model-lab` accepted once on 8 September 2026. The current delivery objective is an externally usable research platform plus three substantive reproducible quality/cost findings, including a candidate-mechanism investigation. The [v1.0 acceptance contract](docs/V1-CONTRACT.md) supersedes the earlier 0.4 ceiling; its unmet gates remain explicit.

Native tooling returned no active goal at this acceptance, so the explicitly requested v1 Goal was created successfully and is **ACTIVE / INCOMPLETE**. Earlier native-goal wording below is historical and does not describe current tooling state. Public v1 release and Tanduna changes require approval of exact prepared artifacts; no new paid resources are authorized.

The 14B main control is complete: 16 arms, 2,048/2,048 exact answers, token parity in all eight balanced pairs and a passing device-specific measured comparison. All 2,048 native streams passed raw reconciliation. Service-throughput medians are 1.740 at concurrency one and 3.857 at concurrency four; these are service metrics, not universal or full-allocation gains. The original producer and redacted export are retained.

The first real v1 proposal→recipe→native run→inspect/report/workbench→crash recovery→verified export workflow is complete using archive03. Its 0.5B reference-chain development quality is adverse (1/8 in each arm), so it establishes product operation rather than research-quality acceptance. A preceding admission failure exposed a 78-byte profile-size error; a new profile version corrects the byte count while keeping the same pinned weights, and the old declaration/failure remain immutable. See [the first workflow](docs/evidence/v1-first-workflow-02.json) and [size correction](docs/decisions/0.5b-profile-size-correction.md).

The three verified GPT-6 Astra investigators have used the actual product in one serialized native queue. The quality investigation is complete: fresh held-out 14B confirmation has 127/128 exact answers in each arm/repetition and preserved token parity, with median service throughput 4.123× and full-attempt efficiency 3.912×. Neither arm meets the frozen joint latency SLO on any of its 256 responses. The candidate's actual fit/calibration produces an ALWAYS_FULL controller because all six frozen thresholds fail acceptance; its matched-record control and adverse development are retained. All three frozen candidate confirmation comparisons are complete; its eight cells retain 1,984 native observations and 68.42 minutes of archive-process execution. Learned deployment selects full context in every case and matches the adverse 23/128 baseline in both repetitions. All five systems cases are complete with 1,600 responses. The held-out 14B slice lowers accuracy from 73/128 to 55/128 while reducing output length and wall time; its final analysis and manuscript are complete, with actual ZIP integration and the selected-cell separate reproduction complete. See [the findings overview](docs/release/FINDINGS-OVERVIEW.md). Root accepts three distinct bounded local findings after the [separate review](docs/research/reproduction-review/report.md); publication and external/human gates remain open.

The latest completed [broad software suite](docs/evidence/automated-tests-v1-platform-06.json) passes 314 tests in 43.543 seconds; all 96 recorded source, test and tool files remain byte-identical to their pre-test snapshot. The local `1.0.0` executable has been built and its deterministic identity and twelve isolated CLI checks pass. [Archive06](docs/evidence/packaged-v1-platform-06.json) adds visible latency-qualified counts, frozen targets and goodput to the versioned workbench; investigators retain immutable archive04. The actual quality workbench exposes 0/256 latency-qualified responses per arm, beside its accuracy. Actual candidate03 native first-use and recovery checks now pass in a fresh Python environment; archive06 workbench browser checks also pass. The technique catalog, proposal/recipe loop, actual controller training, raw evidence gates, recovery and split evidence distribution are implemented. The controller training is a small empirical routing model, not LLM training or a broad novelty claim.

The preliminary package completed actual inference, persistence, export and crash recovery in a separate same-host detached worktree with a six-key minimal environment and reused admitted weights/backend. Its [receipt](docs/evidence/v1-clean-preliminary-workflow.json) records 32 observed comparison requests plus 10 recovery observations from 16 offered recovery requests. The first actual workbench passed native [100%](docs/evidence/v1-preliminary-browser-100.json) and [200%](docs/evidence/v1-preliminary-browser-200.json) browser zoom in an isolated empty network namespace; authentic screenshots are retained. The actual archive06 workbenches now pass [keyboard task segments, native 200% zoom and narrow reflow](docs/evidence/v1-final-workbench-browser.json); the 2,048-row retained study loaded in 496 ms in one observed check. Candidate03 now passes the [actual clean-package workflow](docs/evidence/v1-final-clean-workflow.json): all 19 commands succeed in a new same-host detached worktree and Python 3.12.14 venv created without pip. Its 32 comparison responses and 10 recovery observations are retained in separate exports. [Three-ZIP and source readback](docs/evidence/v1-candidate03-distribution-readback.json) passes every inventory, hash and local source/index link, with documented preserved upstream-notice aliases. The separate verified Astra reproduction completed three full cells and 1,536 new observations. Actual final index review covers all 23 study entries, download hashes and keyboard navigation into the full quality workbench at native 100%/200% zoom and narrow width. The final delivery adds those receipts and separately binds the final source commit and exact artifacts.

The canonical plan has 249 tasks, 32 waves and 472 edges; all 235 earlier task objects and 442 earlier edges were preserved at reconciliation. The v1 catalog has scoped software verification, and the other seven platform tasks plus all three original research matrices have scoped native runtime evidence. Exact ZIP integration and clean-package execution now pass. Separate reproduction and final distribution-index review have scoped local evidence. Exact final delivery records bind source, original scientific artifacts, tested runtime and reviewed pages. Public release, native Tanduna acceptance, external-person use and qualified human reviews remain incomplete. All continued expensive work remains within the original open 12-hour/60GB/12-thread/200GB allocation with one heavy job; the clock has not been extended.

## Retained 0.4 continuation evidence

Lucas authorized a separate 12-hour allocation, decimal 200 GB project disk, 60 GB aggregate RAM and at most twelve inference compute threads on 8 September 2026. The old allocation is closed; its 0.1 archive and adverse result remain frozen. The expanded local target is documented in [the scope decision](docs/decisions/0.4-scope.md) and [the recipe runbook](docs/RUNBOOK-0.4.md).

- **IMPLEMENTED / UNDER INTEGRATED VERIFICATION:** reviewed sharded profiles and configurable immutable recipes; deterministic paced/burst replay with bounded queues and pre-dispatch expiry; offered-denominator quality/SLO goodput; an offline workbench with compatible-family grouping and per-request evidence.
- **AUTOMATED PASS:** latest measurement freeze completed 200 tests, including native inert-server, raw/export, recipe, scheduling, coordinator cancellation, predecessor-measurement, workspace scope and process-lifecycle controls. Test fixtures are not model observations.
- **PACKAGED LOCAL PASS:** frozen `0.4.0.dev0` measurement archive passed the deterministic manifest/implementation and twelve isolated CLI checks. Its exact identity is recorded in [the prospective measurement recipes](docs/decisions/14b-measurement-recipes-01.md) and [archive verification](docs/evidence/packaged-0.4-measurement-verification.json).
- **14B ACQUISITION / BUILD PASS:** all eight reviewed FP16 shards verified by byte size and SHA-256; pinned CPU backend built; actual first-shard tokenizer/template metadata inspected. Development completed 32/32 correct requests across four arms, with both pairs preserving token parity. Development remains ineligible. The main 128-request campaign is running; paced/burst studies and final efficiency interpretation remain pending. See [the development result](docs/results/14b-development-20260908.md).
- **REAL V2 RECOVERY PASS:** a coordinator kill retained two checkpoints; backend cleanup, torn-journal preservation/rebuild and a fresh eight-request replacement passed. Historical v1 reports still produce their original digests. See [the recovery receipt](docs/evidence/14b-recovery-verification.json).
- **PROCESS LIFECYCLE PASS:** actual inert preparation controls verify TERM receipts, KILL cleanup and an inherited lease held until owned descendants finish. Native inference also inherits the same lease. Run/resume reject nested working directories and artifacts outside the counted workspace.
- **BROWSER AUDIT IN PROGRESS:** sandboxed local Chromium launches through a private CDP pipe with no owned TCP listener. Original page captures identified duplicate labels and narrow-layout overflow, now corrected in source. Actual legacy-artifact recaptures, combined keyboard filters, details, reset, empty state and 375px reflow passed. Native 200% browser zoom passed on a tiny control page; applying it to the final v2 artifact remains pending. The earlier workbench content-enlargement check is labeled separately.
- **PLAN CONSISTENCY PASS:** 235 tasks, 30 waves, 442 edges; all 27 historical source objects and 71 historical edges retained. Twenty-nine negative controls pass. These are local projections, not native Tanduna publication.

No 0.4 release gate is complete merely because the version string or test count changed. Real paired serving observations, final artifact review and the remaining public/external/human gates stay explicit. All 24 local-contract tasks remain IN PROGRESS with scoped evidence levels. Two planned serving probes do not complete the broader load-sweep acceptance.

## Architecture foundation accepted

The active root accepted the three foundation tasks in dependency order under Lucas Santana's explicit instruction to complete and publish this first architecture round. The reviewed source is the foundation commit on `main` containing this file, whose parent is `f55f872c74caf0a5fa6f4503042ec22733adc2e3`. This acceptance covers documentation and executable repository-plan tooling. It does not establish scientific validity, implemented simulation, user validation or a released product.

| Task | State | Acceptance evidence |
| --- | --- | --- |
| LM-F01 | **DONE** | Architecture, experiment and source contracts reviewed; scientific boundaries, evaluator isolation and explicit failure semantics accepted as design requirements. |
| LM-F02 | **DONE** | Roadmap and task graph reviewed; all 24 original contracts and eight scientific gates preserved, with only the foundation entry dependency added. |
| LM-F03 | **DONE** | Next-work packet reviewed; python3 tools/validate_plan.py and eight root negative probes passed on the accepted foundation diff. |
- LM-F01: **DONE** — Architecture, experiment and source contracts reviewed; scientific boundaries, evaluator isolation and explicit failure semantics accepted as design requirements.
- LM-F02: **DONE** — Roadmap and task graph reviewed; all 24 original contracts and eight scientific gates preserved, with only the foundation entry dependency added.
- LM-F03: **DONE** — Next-work packet reviewed; python3 tools/validate_plan.py and eight root negative probes passed on the accepted foundation diff.
- Wave 0 review evidence: **ACCEPTED** — active root, 2026-09-07; main foundation commit containing this file, parent f55f872c74caf0a5fa6f4503042ec22733adc2e3; complete diff reviewed, corrections resolved, plan validator and eight negative probes PASS.

## Historical foundation verification

- `python3 tools/validate_plan.py` — **PASS** on the accepted foundation: 27 tasks, nine waves and 71 dependency edges.
- Root negative probes in disposable copies — **PASS**: missing dependency, cycle, malformed dependency, Boolean wave, empty owned paths, wrong project, non-object root and TASKS.md status drift were all rejected cleanly.
- Original-plan comparison against the parent revision — **PASS**: all 24 original task objects retain their IDs, title, wave, acceptance, owned paths, PLANNED state and prior dependencies; LM-001 adds only LM-F03.
- All eight original scientific gates — **PRESERVED**.
- `git diff --check` — **PASS**.

These checks verify plan consistency and failure handling. They do not prove the architecture's scientific validity or any simulation result.

## Retained 0.1 evidence at its original cutoff

The native Goal remains active and incomplete: actual public 0.1, external reproduction and verified native Tanduna publication are required. Root and bounded leaf models were verified as GPT-6 Astra. See `docs/decisions/launch-audit.md` for the dated startup audit and `docs/results/cpu-20260908.md` for current measured evidence.

- **IMPLEMENTED:** immutable standard-library trace/configuration and CLI; native CPU coordinator, raw SSE capture, strict independent evaluator, durable journal/recovery, standalone report and redacted evidence export.
- **AUTOMATED PASS:** 99 discovered tests on Python 3.12.14/Linux x86_64; `docs/evidence/automated-tests.json` records scope and retained log hash. Adversarial identity, token, quality, denominator, raw-export and recovery checks pass. Test-server fixtures remain synthetic.
- **PACKAGED LOCAL PASS:** frozen measured development zipapp `0.1.0.dev0` passed twelve isolated checks including deterministic build, manifest/implementation identity, trace/evaluation/report, overwrite refusal and damaged-journal repair. See `docs/review/packaged-verification.json`. It was locally built, not publicly obtained.
- **REAL INFERENCE VERIFIED; EFFICIENCY INELIGIBLE:** all twelve arm runs and 1,536 requests completed. Every arm scored 99/128 (77.34375%), below the frozen 95% quality gate. All three concurrency-4 pairs failed token parity. Raw/normalized/tokenizer/cache reconciliation passed for 1,536 requests. No comparable-quality speedup is claimed.
- **REAL RECOVERY CONTROL PASS:** coordinator death after five checkpoints stopped the owned backend; torn-journal repair preserved originals; restart used a new attempt ID and completed 128 requests. Both attempts and all costs remain. This is an incomplete control campaign, not another completed comparison.
- **ADVERSE HISTORY RETAINED:** two failed builds and one failed native protocol-validation attempt remain in full costs. The original 30 observed/98 missing protocol attempt was not rewritten. GCC 8 compatibility patch and client correction have explicit provenance.
- **VISUAL RUNTIME AT THE 0.1 CUTOFF:** NOT TESTED. Chromium then lacked a required shared library. Later 0.4 browser observations are separately dated above; they do not retroactively supply that observation.
- **USER / QUALIFIED REVIEW:** NOT TESTED. Agent code/evidence review is not a participant observation or independent domain validation.
- **PUBLIC RELEASE:** NOT RELEASED. No push, tag, release or external distribution performed. Local source/CLI and sanitized evidence packaging is preparation only.
- **TANDUNA:** the 219-task canonical export is generated and validated. The fresh public read on 2026-09-08 found 0 tasks, no published roadmap and one revision-1 Discussion proposal. No native publication IDs are invented; fresh reconciliation is required before writes.

## Retained 0.1 decisions and continuing release gates

Lucas explicitly approved the pinned model/backend and bounded local allocation on 2026-09-08. `docs/decisions/admission-approved-20260908.json` is the actual record; the false historical proposal remains preserved. Acquisition verified source/model hashes and GGUF/tokenizer metadata. The existing GCC 8 toolchain needed a reviewed source-compatibility patch and filesystem link flag, with no new compiler or production dependency. See `docs/decisions/gcc8-build-recovery.md`.

The 128-request/95% quality contract was frozen before inference. The failed result itself did not authorize a model change, another allocation or a lower threshold. The later explicit owner authorization is recorded separately in the active 0.4 section above. All main-comparison observations and the separate recovery control used the original allocation. Final export/report timing belongs to an explicit closing receipt and never extends the producer's measured clock.

At the 0.1 cutoff the canonical programme had 219 tasks, 28 waves and 411 dependency edges. The active 0.4 programme above has 235 tasks, 30 waves and 442 edges. All 27 historical source objects and 71 edges remain immutable. New task evidence records distinguish automated checks and scoped local runtime from full acceptance. Training, analytical calibration and broader historical scientific contracts remain incomplete.

Remaining release gates at the 0.1 cutoff: a concrete artifact/destination publication decision and Git author identity; an authenticated supported Tanduna session plus real submission/review/poll outcomes; a fresh authorized external environment obtaining and exercising the public artifact; actual qualified/researcher observations; and browser/accessibility runtime checks. A prepared archive or pending platform proposal does not satisfy these gates.

Wave 6 now contributes the bounded local 0.3 arrival/service continuation; its broader load-sweep and qualified-review acceptance remains open. No training, new kernel or GPU work was added to either measured increment.
